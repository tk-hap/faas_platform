{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "faas-platform.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "faas-platform.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
