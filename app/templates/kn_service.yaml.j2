apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: {{ language }}-{{ tag }}
spec:
  template:
    spec:
      containers:
        - image: {{ registry }}:{{ tag }}
          ports:
            - containerPort: 8080